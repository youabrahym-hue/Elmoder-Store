# Elmoder Store - Online Update

- Added Excel `.xlsx` import into the devices table with duplicate-IMEI protection.
- Added Excel export/open controls on the Devices page.
- Added a safe Excel file clear control (clears the selected upload file only; it does not delete database records).
- Added online database-backed backup records, so backups work with PostgreSQL/online deployments.
- Added backup download, restore-from-JSON, and delete controls.
- Preserved the existing Elmoder Store logo and kept it in the sidebar, mobile header, login screen, and device header.
